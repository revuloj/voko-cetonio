% metu la agordo-dosieron sub ${HOME}/etc

% redaktilo-servo
http_app_root('/redaktilo').
http_app_scheme(http).
http_app_host('example.com').
http_app_port(8080).

% citaĵo-servo
http_cit_root('/citajhoj').
http_cit_host('example.com').
http_cit_port(9090).
http_cit_scheme(http).

% analizo-servo
http_ana_root('').
http_ana_host('example.com').
http_ana_port(8081).
http_ana_scheme(http).

http_session_timeout(3600).

% retpoŝto-sendo
mail_from(<retadreso de redaktilo>).
mail_addr(<retadreso de redaktoservo>).

mail_smtp(<retadreso de poŝtservo>).
mail_security(starttls).
mail_user(<uzanto de sendanta poŝtfako>).
mail_pw(<pasvorto de sendanta poŝtfako>).
mail_auth(login). % unu el plain, login, none, default

mail_addr_test(<celadreso por testo>).
mail_reply_test(<respondadreso por testo>).

% sekretoj
ajax_secret(<iom longa arbitra signaro>).
:- multifile google_client:key/2.
google_client:key(client_id,'<hash>.apps.googleusercontent.com').
google_client:key(client_secret,<sekreto>).


%%% padoj

voko_xsl(<.../revohtml2.xsl>).
redaktantoj_import('<.../redaktantoj>').

revodb_zip('http://retavortaro.de/tgz/revosql-inx_~w.zip').
revodb_tmp('/pado/al/revo-inx.db.tmp.zip').

web_dir(<.../web>).
voko_dir(<.../voko>).

revodb(<sqlite-datumbazo kun indeksoj por serĉoj>).
kontodb(<sqlite-datumbazo de redaktanto-kontoj>).

relax_rnc(<RelaxNG RNC Schema (por Jing)>).
tekstoj(<Pado por tekstoj (Biblio, Monato)>).
verkoj(<Pado por TEIXLite textoj kaj DTD>).

